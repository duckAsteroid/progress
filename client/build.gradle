plugins {
    id 'duckasteroid'
}
description = 'Client library for framework reporting'

dependencies {
    implementation project(':api')
    testImplementation(libs.junit)

    // Mockito
    testImplementation(libs.mockito)
}


